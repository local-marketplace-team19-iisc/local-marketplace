# IDE
.vscode/
.idea/

copilotBytesTracker/

CLAUDE.local.md

# Active feature context (Constitution P7) — local-only, never committed
.active_feature

# Secrets (Constitution P4) — .env.example is committed, .env is not
.env
.venv/

.pytest_cache
.ruff_cache

__pycache__

# Environments and Build Artifacts
*.egg-info/
.eggs/

# Frontend-local ignores (keeps root .gitignore untouched — Constitution P6)
node_modules/
build/
dist/
.env
*.local
.eslintcache
npm-debug.log*
.git
*.log
# Feature 002 — Conversational agent
# Pre-downloaded Whisper weights (large; documented as a fetch step in README)
# Anchored to repo root so it does NOT collide with backend/app/models/ (ORM).
/models/
# Local trace / log directory
traces/

# Local SQLite dev databases (runtime data, not code)
*.db
.vercel

.wibey
