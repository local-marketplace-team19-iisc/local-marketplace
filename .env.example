# Port the API server binds to (default 8000)
PORT=8081

# PostgreSQL + PostGIS connection string (feature 003-vendor-customer-auth)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/local_marketplace
